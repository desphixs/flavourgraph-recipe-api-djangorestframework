from django.urls import path
# We import path from django.urls to define URL routes for our API

# This list will hold the URL patterns for the recipes application.
# Currently, it is empty as we haven't created any views yet, but we will add routes here in future tasks!
urlpatterns = [
    # Example: path('route/', ViewName.as_view(), name='route-name'),
]
